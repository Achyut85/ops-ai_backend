[
  {
    "id": "column.public.ticket.deletedAt",
    "label": "Add column \"deletedAt\" to \"ticket\"",
    "operationClass": "additive",
    "target": {
      "id": "postgres",
      "details": {
        "schema": "public",
        "objectType": "column",
        "name": "deletedAt",
        "table": "ticket"
      }
    },
    "precheck": [
      {
        "description": "ensure column \"deletedAt\" is missing",
        "sql": "SELECT NOT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "ticket",
          "deletedAt"
        ]
      }
    ],
    "execute": [
      {
        "description": "add column \"deletedAt\"",
        "sql": "ALTER TABLE \"public\".\"ticket\" ADD COLUMN \"deletedAt\" timestamptz"
      }
    ],
    "postcheck": [
      {
        "description": "verify column \"deletedAt\" exists",
        "sql": "SELECT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "ticket",
          "deletedAt"
        ]
      }
    ]
  }
]